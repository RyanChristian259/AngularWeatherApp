import { Component } from '@angular/core';

/**
 * Note that this file is empty because this app is meant to
 * demonstrate the weather widget as a stand alone widget within
 * a full featured app. As it is written now, the widget could be
 * used in another app or left where it is.
 */

@Component({
  selector: 'app-weather',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss']
})
export class AppComponent {}
